# Normalize text to LF in the repo (clean for macOS/Linux friends); auto-detect.
* text=auto eol=lf

# Binary assets — never apply end-of-line conversion (prevents PDF/XLSX corruption on checkout).
*.pdf  binary
*.xlsx binary
*.xls  binary
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
